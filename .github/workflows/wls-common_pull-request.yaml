name: verify pull request wls-common

on:
  pull_request:
    paths:
      - 'wls-common/**'

jobs:
  verify-pull-request:
    permissions:
      contents: read
    uses:
      ./.github/workflows/callable-run-mvn-verify.yml
    with:
      pom-dir: 'wls-common'
      javaVersion: '21'
