name: build push dev gui admintool

on:
  push:
    branches:
      - dev
    paths:
      - 'wls-gui-admintool/**'
      - '.github/workflows/wls-gui-admintool_push-dev.yml'

jobs:
  build-github-container-image:
    permissions:
      packages: write
    uses:
      ./.github/workflows/callable-create-github-container-image-frontend.yml
    with:
      service: 'wls-gui-admintool'