name: verify pull request gui-wahllokalsystem

on:
  pull_request:
    paths:
      - 'wls-gui-wahllokalsystem/**'
      - '.github/workflows/wls-gui-wahllokalsystem_pull-request.yml'

jobs:
  verify-pull-request:
    uses:
      ./.github/workflows/callable-run-npm-build.yml
    with:
      package-dir: 'wls-gui-wahllokalsystem'
