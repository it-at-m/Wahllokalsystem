name: build push dev gui wahllokalsystem

on:
  push:
    branches:
      - dev
    paths:
      - 'wls-gui-wahllokalsystem/**'
      - '.github/workflows/wls-gui-wahllokalsystem_push-dev.yml'

jobs:
  build-github-container-image:
    permissions:
      packages: write
    uses:
      ./.github/workflows/callable-create-github-container-image-frontend.yml
    with:
      service: 'wls-gui-wahllokalsystem'