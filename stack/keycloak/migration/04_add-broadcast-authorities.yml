id: add broadcast authorities
author: MrSebastian
realm: ${SSO_REALM}
changes:
  - addRole:
      name: Broadcast_BUSINESSACTION_Broadcast
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Broadcast_BUSINESSACTION_GetMessage
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Broadcast_BUSINESSACTION_MessageRead
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Broadcast_DELETE_Message
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Broadcast_READ_Message
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Broadcast_WRITE_Message
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
