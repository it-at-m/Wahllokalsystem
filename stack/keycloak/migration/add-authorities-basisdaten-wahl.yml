id: add authorities basisdaten wahl
author: GerhardPx
realm: ${SSO_REALM}
changes:
  - addRole:
      name: Basisdaten_BUSINESSACTION_GetWahl
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allBasisdatenAuthorities
      role: Basisdaten_BUSINESSACTION_GetWahl
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Basisdaten_BUSINESSACTION_PostWahl
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allBasisdatenAuthorities
      role: Basisdaten_BUSINESSACTION_PostWahl
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Basisdaten_READ_Wahl
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allBasisdatenAuthorities
      role: Basisdaten_READ_Wahl
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Basisdaten_WRITE_Wahl
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allBasisdatenAuthorities
      role: Basisdaten_WRITE_Wahl
      clientId: ${SSO_CLIENT_ID}

