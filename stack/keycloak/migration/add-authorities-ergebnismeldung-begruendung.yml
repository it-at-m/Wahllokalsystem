id: add authorities ergebnismeldung begruendung
author: GerhardPx
realm: ${SSO_REALM}
changes:
  - addRole:
      name: Ergebnismeldung_BUSINESSACTION_GetBegruendung
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_BUSINESSACTION_GetBegruendung
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Ergebnismeldung_BUSINESSACTION_PostBegruendung
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_BUSINESSACTION_PostBegruendung
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Ergebnismeldung_READ_Begruendung
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_READ_Begruendung
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Ergebnismeldung_WRITE_Begruendung
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_WRITE_Begruendung
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Ergebnismeldung_DELETE_Begruendung
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_DELETE_Begruendung
      clientId: ${SSO_CLIENT_ID}