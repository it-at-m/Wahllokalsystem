id: add authorities ergebnismeldung wahlscheine
author: dragonfly28
realm: ${SSO_REALM}
changes:
  - addRole:
      name: Ergebnismeldung_BUSINESSACTION_GetWahlscheine
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_BUSINESSACTION_GetWahlscheine
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Ergebnismeldung_BUSINESSACTION_PostWahlscheine
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_BUSINESSACTION_PostWahlscheine
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Ergebnismeldung_READ_Wahlscheine
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_READ_Wahlscheine
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Ergebnismeldung_WRITE_Wahlscheine
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_WRITE_Wahlscheine
      clientId: ${SSO_CLIENT_ID}
  - addRole:
      name: Ergebnismeldung_DELETE_Wahlscheine
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allErgebnismeldungAuthorities
      role: Ergebnismeldung_DELETE_Wahlscheine
      clientId: ${SSO_CLIENT_ID}