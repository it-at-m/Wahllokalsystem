id: add authorities monitoring wahllokalzustand
author: GerhardPx
realm: ${SSO_REALM}
changes:
  - addRole:
      name: Monitoring_BUSINESSACTION_PostLastSeen
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allMonitoringAuthorities
      role: Monitoring_BUSINESSACTION_PostLastSeen
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Monitoring_BUSINESSACTION_PostLetzteAbmeldung
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allMonitoringAuthorities
      role: Monitoring_BUSINESSACTION_PostLetzteAbmeldung
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Monitoring_BUSINESSACTION_PostSchnellmeldungSendungsuhrzeit
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allMonitoringAuthorities
      role: Monitoring_BUSINESSACTION_PostSchnellmeldungSendungsuhrzeit
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Monitoring_BUSINESSACTION_PostSchnellmeldungDruckuhrzeit
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allMonitoringAuthorities
      role: Monitoring_BUSINESSACTION_PostSchnellmeldungDruckuhrzeit
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Monitoring_BUSINESSACTION_PostNiederschriftSendungsuhrzeit
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allMonitoringAuthorities
      role: Monitoring_BUSINESSACTION_PostNiederschriftSendungsuhrzeit
      clientId: ${SSO_CLIENT_ID}

  - addRole:
      name: Monitoring_BUSINESSACTION_PostNiederschriftDruckuhrzeit
      clientRole: true
      clientId: ${SSO_CLIENT_ID}
  - assignRoleToGroup:
      group: allMonitoringAuthorities
      role: Monitoring_BUSINESSACTION_PostNiederschriftDruckuhrzeit
      clientId: ${SSO_CLIENT_ID}