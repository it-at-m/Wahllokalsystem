### get token for wls_all_bwb
POST http://{{ SSO_URL }}/auth/realms/wls_realm/protocol/openid-connect/token
Content-Type: application/x-www-form-urlencoded

password = test &
grant_type = password &
client_secret = top-secret &
client_id = wls &
username = wls_all_bwb

> {%
    client.global.set("auth_token", response.body.access_token);
    client.global.set("token_type", response.body.token_type);
%}

### get token for wls_all_uwb
POST http://{{ SSO_URL }}/auth/realms/wls_realm/protocol/openid-connect/token
Content-Type: application/x-www-form-urlencoded

password = test &
grant_type = password &
client_secret = top-secret &
client_id = wls &
username = wls_all_uwb

> {%
    client.global.set("auth_token", response.body.access_token);
    client.global.set("token_type", response.body.token_type);
%}

### get userinfo with auth_token
GET http://{{ SSO_URL }}/auth/realms/wls_realm/protocol/openid-connect/userinfo
Authorization: {{ token_type }} {{ auth_token }}

### get konfigration WILLKOMMENSTEXT
GET http://localhost:{{ WLS_INFOMANAGEMENT_SERVICE_PORT }}/businessActions/konfiguration/WILLKOMMENSTEXT
Authorization: {{ token_type }} {{ auth_token }}

### POST konfigration WILLKOMMENSTEXT
POST http://localhost:39146/businessActions/konfiguration/WILLKOMMENSTEXT
Authorization: {{ token_type }} {{ auth_token }}
Content-Type: application/json

{
  "wert": "neuer Willkommenstext",
  "beschreibung": "neuer Beschreibung",
  "standardwert": "neuer Standardwert"
}

### get konfigration FRUEHESTE_EROEFFNUNGSZEIT
GET http://localhost:{{ WLS_INFOMANAGEMENT_SERVICE_PORT }}/businessActions/konfiguration/FRUEHESTE_EROEFFNUNGSZEIT
Authorization: {{ token_type }} {{ auth_token }}

### get KENNBUCHSTABEN
GET http://localhost:{{ WLS_INFOMANAGEMENT_SERVICE_PORT }}/businessActions/kennbuchstaben
Authorization: {{ token_type }} {{ auth_token }}

### get wilkommenstext as anonymous
GET localhost:{{ WLS_INFOMANAGEMENT_SERVICE_PORT }}/businessActions/konfigurationUnauthorized/WILLKOMMENSTEXT